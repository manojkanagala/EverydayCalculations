<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/discount"
    app:startDestination="@id/nav_discount">

    <fragment
        android:id="@+id/nav_discount"
        android:label="@string/discount"
        android:name="com.manukanagala.everydaycalculations.fragments.DiscountFragment"
        tools:layout="@layout/fragment_discount">
        <action
            android:id="@+id/discount_fragment"
            app:destination="@id/discount" />
    </fragment>

</navigation>